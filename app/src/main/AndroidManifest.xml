<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qrapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QrApplication">
        <activity android:name=".Inbox"></activity>
        <activity
            android:name=".QR"
            android:label="@string/title_activity_q_r"
            android:theme="@style/Theme.QrApplication.NoActionBar" />
        <activity
            android:name=".profilepage"
            android:label="@string/title_activity_profilepage"
            android:theme="@style/Theme.QrApplication.NoActionBar" />
        <activity
            android:name=".home"
            android:label="@string/title_activity_share_qr"
            android:theme="@style/Theme.QrApplication.NoActionBar" />
        <activity android:name=".Signup" />
        <activity android:name=".readyToGo" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>